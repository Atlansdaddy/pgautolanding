# Normalize line endings to LF in the repo (avoids CRLF churn on Windows).
* text=auto eol=lf

# Treat binary assets as binary (no EOL normalization, no diff).
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.avif binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
